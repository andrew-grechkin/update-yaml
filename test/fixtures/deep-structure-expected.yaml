service:
  metrics:
    interval: 60
    port: 9090
    path: /metrics
  name: api
  config:
    cache:
      size: 2048
      enabled: true
    timeout: 30
    logging:
      format: json
      level: info
    retries: 3
